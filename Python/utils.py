import numpy as np
from math import tanh

def sigmrnd(X):
    X = 1/(1+np.exp(-X)) > rand(P.shape)
    return X

def sigm(X):
    return 1 / (1+np.exp(-X))

def softmax(X):
    return np.exp(X) / np.sum(np.exp(X), axis=1)

def tanh(X):
    return 1.7159 * tanh(2/3*A);

def linear(X):
    return X
